[project]
name = "focusassist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai",
    "ollama",
    "Pillow",
    "pytesseract",
    "python-dotenv",
    "pywin32"
]
